
		<link
			rel="stylesheet"
			href="https://use.fontawesome.com/releases/v5.7.0/css/all.css"
			integrity="sha384-lZN37f5QGtY3VHgisS14W3ExzMWZxybE1SJSEsQp9S+oqd12jhcu+A56Ebc1zFSJ"
			crossorigin="anonymous">
		<link
			rel="stylesheet"
			href="/stylesheets/main.css">
	</head>
	<body>
		<nav class="navbar shadow sticky-top navbar-expand-md navbar-default navbar-dark bg-danger">
			<div class="navbar-header">
				<a class="navbar-brand" href="/">TechniApp <i class="fas fa-rocket"></i></a>
			</div>
			<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
				<span class="navbar-toggler-icon"></span>
			</button>
			<div class="collapse navbar-collapse" id="navbarNav">
				<% if(!currentUser){ %>
				<ul class="navbar-nav ml-auto">

						<li class="nav-item"><a class="nav-link" href="/login">Login</a></li>
						<!--<li class="nav-item"><a class="nav-link" href="/register">Register</a></li>-->
				</ul>
				<% } else { %>
				<ul class="navbar-nav mr-auto">
					<li class="nav-item">
						<a class="nav-link" href="/jobs">Jobs</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="/jobs/archive">Archive</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="/users">Users</a>
					</li>
				</ul>
				<ul class="navbar-nav">
					<li class="nav-item"><span class="mr-2 navbar-text">Hi <%= currentUser.firstName %></span></li>
					<li class="nav-item"><a class="nav-link" href="/logout">Logout</a></li>
				</ul>
				<% } %>
			</div>
		</nav>

		<% if(error && error.length > 0){ %>
			<div class="container">
				<div class="alert alert-danger shadow-sm mt-3 mx-md-5" role="alert">
					<% if(error.length === 1) { %>
						<%= error %>
					<% } else { %>
						<ul class="list-group">
							<% error.forEach(function(err) { %>
								<li class="list-group-item bg-transparent border-0 p-0"> <%= err %> </li>
							<% }) %>
						</ul>
					<% } %>
				</div>
			</div>
		<% } %>

		<% if(success && success.length > 0){ %>
			<div class="container">
				<div class="alert alert-success shadow-sm mt-3 mx-md-5" role="alert">
					<%= success %>
				</div>
			</div>
		<% } %>

		<div class="container my-4">